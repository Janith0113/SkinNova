# SkinNova - Frontend

This is a starter Next.js (App Router) + TypeScript + Tailwind CSS frontend.

Quick start (PowerShell):

```powershell
cd frontend
npm install
npm run dev
```

App runs at http://localhost:3000
