# SkinNova - Backend

Simple Node.js + Express backend written in TypeScript.

Quick start (PowerShell):

```powershell
cd backend
npm install
npm run dev
```

API:
- GET /api/health - returns basic health status
